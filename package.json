{
  "name": "google-contacts-viewer",
  "version": "1.0.0",
  "description": "A secure Google Contacts Viewer application",
  "scripts": {
    "build": "node build.js && npm run verify",
    "verify": "node verify-build.js",
    "dev": "http-server -p 8888"
  },
  "dependencies": {},
  "devDependencies": {
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
